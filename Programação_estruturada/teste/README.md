# Calculadora de Média com parametros

Esse programa calcula a média de n valores passando os argumentos via linha de comando.

## Como usar o programa

Compile o programa usando ` gcc media.c -o media ` e execute com ` ./media VAL1 VAL2 [VAL3...] `